# Deploying Frontend

This frontend is static and can be hosted on any static hosting service (GitHub Pages, Netlify, Vercel, etc.).

## Local Preview

```bash
cd frontend
python -m http.server 8080
```

Open http://localhost:8080
