# DonationBox Frontend (Minimal)

This is a lightweight frontend demo for DonationBox.

## Running

```bash
cd frontend
python -m http.server 8080
```

Then open `http://localhost:8080`.

## Notes

- Uses [Freighter](https://www.freighter.app/) for Stellar wallet connection.
- Donations are stored locally in `localStorage`.
